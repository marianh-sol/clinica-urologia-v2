services:
  - type: web
    name: clinica-urologia
    runtime: static
    buildCommand: pnpm install && pnpm build:static
    publishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html


